<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <link href="@Url.Content("~/Content/bootstrap.min.css")" rel="stylesheet" type="text/css" />
    <link href="@Url.Content("~/Content/bootstrap-responsive.min.css")" rel="stylesheet" type="text/css" />
    <link href="@Url.Content("~/Content/site.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Scripts/jquery-1.7.2.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/bootstrap.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/site.js")" type="text/javascript"></script>
</head>
<body>
    <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
            <div class="container">
                <a class="brand" href="@Url.Content("~/")">@(ViewBag.Brand ?? "Postworthy")</a>
                <ul class="nav">
                    <li class="@((ViewBag.Home == true) ? "active" : "")"><a href="@Url.Content("~/")"><i class="icon-home @((ViewBag.Home == true) ? "icon-white" : "")"></i> Home</a></li>
                    <li class="@((ViewBag.About == true)? "active" : "")"><a href="@(Url.Content("~/home/about"))"><i class="icon-info-sign @((ViewBag.About == true) ? "icon-white" : "")"></i> About</a></li>
                </ul>
                <ul class="nav pull-right">
                    @Html.Partial("_LogOnPartial")
                </ul>
            </div>
        </div>
    </div>
    <div class="container">
        @RenderBody()        
        <hr>
        <footer>
            <p>
                &copy; @(ViewBag.Brand ?? "Postworthy") @DateTime.Now.Year powered by <a target="_blank" href="http://postworthy.org" title="Postworthy - The Open Source Content Curation Tool">Postworthy</a>
            </p>
        </footer>
    </div>
    @Html.Raw(Postworthy.Models.Account.UsersCollection.PrimaryUser().AnalyticsScript ?? "")
</body>
</html>
